Tengo una carpeta assets con fotos, cada foto tiene un nombre único

Al encender el programa debe hacer una carga inicial, debe hacer una llamada a la api de Azure para generar los tags de cada foto,
una vez extraidos los tags guardaremos en un diccionario 
                                        {key -> nombreImagen : valor -> [tags]}

Despues de la carga inicial, le mostraremos textos e inputs para que pueda introducir una foto y un boton para que pueda lanzar 
el proceso de deteccion de similitudes.
El input de la imagen podría estar ligado a un chace de streamlit para mejorar el rendimiento.


# # Solicitamos una imagen
# with col1:
#     imagen = st.file_uploader("Introduce una imagen:", type=['png', 'jpg'])
# with col2:
#     if imagen is not None:
#         st.image(imagen, caption="Vista previa", use_container_width=True)
#     else:
#         st.text("No has introducido ninguna imagen")
        
# botonSubmit = st.button("Analizar imagen:")

# # Una vez introducida la imagen
# if botonSubmit:
#     if imagen is not None:
#         image_data = imagen.read()

#         try:
#             response = requests.post(
#                 endpoint,
#                 headers=headers,
#                 params=params,
#                 data=image_data
#             )
#             response.raise_for_status()

#             result = response.json()
#             st.json(result) 
            
#             captions = result['tags']
#             for caption in captions:
#                 st.success(f"{caption['name']} (confianza: {caption['confidence']:.2%})")
            
#         except Exception as ex:
#             st.error(f"Error al analizar la imagen: {ex}")
#     else:
#         st.error("No has introducido ninguna imagen.")